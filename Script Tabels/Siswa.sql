CREATE TABLE Siswa(
    SiswaId INT IDENTITY(1,1) NOT NULL,
    NamaLengkap VARCHAR(50) NOT NULL DEFAULT(''),
    NamaPanggil VARCHAR(20) NOT NULL DEFAULT(''),
    TmpLahir VARCHAR(20) NOT NULL DEFAULT(''),
    TglLahir DATETIME NOT NULL DEFAULT('3000-01-01'),
    Gender INT NOT NULL DEFAULT(0),
    Agama VARCHAR(15) NOT NULL DEFAULT(''),
    Kewarganegaraan VARCHAR(10) NOT NULL DEFAULT(''),
    NIK VARCHAR(20) NOT NULL DEFAULT(''),
    AnakKe INT NOT NULL DEFAULT(0),
    JmlhSdrKandung INT NOT NULL DEFAULT(0),
    JmlhSdrTiri INT NOT NULL DEFAULT(0),
    JmlhSdrAngkat INT NOT NULL DEFAULT(0),
    YatimPiatu VARCHAR(20) NOT NULL DEFAULT(''),
    Bahasa VARCHAR(30) NOT NULL DEFAULT(''),
    Alamat VARCHAR(50) NOT NULL DEFAULT(''),
    NoTelp VARCHAR(20) NOT NULL DEFAULT(''),
    TngglDengan VARCHAR(20) NOT NULL DEFAULT(''),
    JrkKeSekolah INT NOT NULL DEFAULT(0),
    TransportSekolah VARCHAR(30) NOT NULL DEFAULT(''),

    CONSTRAINT PK_Siswa PRIMARY KEY CLUSTERED(SiswaId)
);
